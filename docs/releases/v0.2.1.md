## What changed

- This is the corrected first publication candidate for the onboarding and
  Linux-repository work prepared as v0.2.0. The immutable `v0.2.0` tag was
  rejected before publication because one Debian extended-description line
  exceeded Lintian's limit; no v0.2.0 release or repository was published.
  Debian metadata is now length-bounded and Lintian runs in protected CI.
- A verified interactive install can continue directly through guided
  provider setup, service activation, health checks, and the first durable
  chat. Running `mealyctl` in a terminal provides the same guided entry point.
- Guided provider choices cover strictly free OpenRouter models, authenticated
  custom OpenAI-compatible endpoints, credentialless loopback servers,
  ChatGPT subscriptions through the official Codex client, and advanced
  OpenAI or Anthropic API configuration. Direct Claude subscription-token
  routing remains unsupported.
- The terminal chat supports durable continuation, a bounded recent-chat
  picker, live provider/model health, configured limits, exact usage and cost
  reporting, and an explicit provider-management handoff.
- `install-status`, `update`, `update-status`, `repair`, `rollback`,
  `uninstall`, and shell completion expose the installed owner and keep
  archive updates transactional. Failed same-schema archive activation is
  automatically restored through the previous verified package slot.
- Ubuntu, Debian, Fedora, and Arch users gain signed APT, DNF, and Pacman
  repository construction and public trust verification in addition to the
  existing attested rootless archives and native packages.
- Public release acceptance repeats the complete install-to-first-chat,
  service restart, durable continuation, diagnostics, and uninstall journey
  using only downloaded release assets.
- Persistent user services reject homes on `/tmp`, `/var/tmp`, `tmpfs`, and
  `ramfs`; release package tests exercise them from an explicit persistent
  container path.

## Publication qualification

The immutable v0.2.1 tag and 17 digest-bearing assets were published by
[release run 30315187057](https://github.com/Amekn/mealy/actions/runs/30315187057). Its builds,
soak-subject promotion, x86-64 and ARM64 packages, attestations, repository signing, Pages
deployment, rootless acceptance, and native Ubuntu, Debian, Fedora, and Arch package tests passed.
Five dependent repository jobs then stopped before downloading their attestation because a
checkout-free GitHub CLI invocation omitted the explicit repository argument.

That was a verifier-harness defect rather than a shipped-content defect. The remediation was merged
through [PR #63](https://github.com/Amekn/mealy/pull/63), and all 16 protected-main checks passed
for exact remediation commit `d421144e720895ea2f6266df097f51215263815c` in
[CI run 30324063774](https://github.com/Amekn/mealy/actions/runs/30324063774). The protected,
non-publishing [post-publication acceptance run
30324688498](https://github.com/Amekn/mealy/actions/runs/30324688498) then qualified the unchanged
tag, source commit `b8e9d8576f228fd43a523ad38704a86b4630b115`, original GitHub attestation,
signed public manifest, and Pages content. Its release qualifier, Ubuntu 24.04 x86-64, Debian 13
ARM64, Fedora 44 x86-64 and ARM64, Arch Linux x86-64, and aggregate jobs all passed.

v0.2.1 is therefore fully qualified for its documented Linux production contract. The immutable
tag, release assets, checksums, SBOMs, signatures, attestations, and repository content were not
changed during remediation. The original run remains red as transparent negative evidence of the
checkout-free GitHub CLI defect; the checked release and repository operations documents retain
the verifier-only exception, fail-closed boundaries, and rerun procedure.
